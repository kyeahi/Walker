Deployment.yaml
      Django pod 를 정의 하는 .yaml 파일 입니다.

Dockerfile
      python, Django 설정 파일 입니다

install.config
      Dockerfile 을 만들고 build 해서 image로 만든 후 deploy.yaml로 kubernetes pod 로 만드는 과정이 있습니다 
