apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: MLops-django
  name: MLops-django
spec:
  replicas: 1
  selector:
    matchLabels:
      app: MLops-django
  template:
    metadata:
      labels:
        app: MLops-django
    spec:
      containers:
      - image: kube4team/test-django-jk:1.0
        name: MLops-django
